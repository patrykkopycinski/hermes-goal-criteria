name: goal-criteria
version: "1.0.0"
description: "Lets the agent read and add acceptance criteria (/subgoal) and quality gates (/goal gate) to the active goal, so a /goal loop runs against explicit, checkable criteria instead of a vague objective."
author: patryk
provides_tools:
  - goal_criteria
